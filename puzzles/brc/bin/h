#!/usr/bin/env bash

set -euo pipefail
DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd)"
cd "$DIR/.."

case "$1" in
  gen)
    rm -r _work
    mkdir _work
    clj -M -m generate _work/measurements.txt 10000
    ;;
  *)
    echo "Unknown command: $1"
    exit 1
esac
